# Dependencies
node_modules/
*/node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo
*/dist/
*/build/
frontend/public/bundle-info.json

# Android (removed - Web PWA only)
# Keeping minimal Android ignores in case of future needs
*.apk
*.aab

# Environment variables
.env
.env.local
.env.*.local
*/.env
*/.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
coverage/
.nyc_output/
**/test-results/
**/test-results.json

# Temporary files
tmp/
temp/
**/tmp/

# Enforcement lock backups (keep lock directory tracked, but ignore backups)
.enforcement-lock/*.backup.*
.enforcement-lock/unlock-log.txt

# Keep checksums.txt tracked (required for validation)
!.enforcement-lock/checksums.txt
!.enforcement-lock/README.md

# Test lock logs (ignore logs, but keep lock files tracked)
.test-locks/*.log
.test-locks/lock-log.txt
.test-locks/unlock-log.txt

# Keep test lock files tracked (required for validation)
!.test-locks/**/*.lock


